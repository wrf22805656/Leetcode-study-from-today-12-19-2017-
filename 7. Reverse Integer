#!/usr/bin/python

def reverse(num):

    rev = 0
    while (num >0):
        rev = 10*rev + num%10
        num //=10

    return rev

number = int(input("Please enter an integer:  "))

print (reverse(number))
